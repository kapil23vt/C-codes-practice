#include <stdio.h>
int main()
{
    int *a = 0;
    int *b = 0;

    // a and b both are NULL pointers

    if(a == b){
        printf("a and b are both null pointers");
    }
    return 0;
}
